# Combine a greeting and a name into a single print statement using variables.

def greet_name(greet, name):
    print(str(greet) + ", " + str(name))

greet = "Hello good morning"
name = "Renz"

greet_name(greet, name)
